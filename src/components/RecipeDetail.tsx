import type { Recipe } from '../types'
export function RecipeDetail({ recipe, favorite, onFavorite, onClose }: { recipe: Recipe; favorite: boolean; onFavorite: () => void; onClose: () => void }) { return <div className="overlay"><article className="modal detail"><div className="modal-head"><h2>{recipe.name}</h2><button className="icon-button" onClick={onClose}>×</button></div><p>{recipe.description}</p><div className="nutrition"><span>{recipe.calories} kcal</span><span>{recipe.protein}g proteine</span><span>{recipe.carbs}g carbo</span><span>{recipe.fat}g grăsimi</span></div><button onClick={onFavorite}>{favorite ? '★ Elimină din favorite' : '☆ Adaugă la favorite'}</button><h3>Ingrediente · {recipe.servings} porții</h3><ul>{recipe.ingredients.map((item, index) => <li key={index}>{item.quantity} {item.unit} {item.name}</li>)}</ul><h3>Preparare</h3><ol>{recipe.steps.map((step, index) => <li key={index}>{step}</li>)}</ol></article></div> }
