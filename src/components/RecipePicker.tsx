import type { Recipe } from '../types'
export function RecipePicker({ recipes, onPick, onClose }: { recipes: Recipe[]; onPick: (recipe: Recipe) => void; onClose: () => void }) { return <div className="overlay"><div className="modal"><div className="modal-head"><h2>Alege o rețetă</h2><button className="icon-button" onClick={onClose}>×</button></div>{recipes.length ? recipes.map((recipe) => <button className="picker" key={recipe.id} onClick={() => onPick(recipe)}><strong>{recipe.name}</strong><small>{recipe.category} · {recipe.servings} porții</small></button>) : <p>Nu există încă rețete.</p>}</div></div> }
